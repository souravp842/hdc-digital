query CartTransformRunInput {
  cart {
    attribute(key: "utm_source") {
      value
    }
    lines {
      id
      quantity
      merchandise {
        ... on ProductVariant {
          product {
            metafield(namespace: "custom", key: "utm_prices") {
              value
            }
          }
        }
      }
    }
  }
}
